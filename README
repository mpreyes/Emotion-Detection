


install conda, create an environment 

pip install numpy
pip install Pillow
pip install opencv-python
pip install keras
pip install tensorflow
source activate local: emotion_detection hpc: emotion_research
source deactivate

other libraries:
scikit-learn


reasearch.py is the main driver for all the code.
